<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techpp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@drawable/logo_bhaichara"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/logo_bhaichara"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BillingActivity"></activity>
        <activity android:name=".RideAddedDriverInterCity" />
        <activity android:name=".DriverConfirmedInterCity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".DriverSignUp" />
        <activity android:name=".InterCityRiderActivity" />
        <activity android:name=".InterCityDriverActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".TransactionHistory" />
        <activity android:name=".RideHistory" />
        <activity android:name=".ProfileSettings" />
        <activity android:name=".LandingActivity" />
        <activity android:name=".PrefrenceActivity" />
         <activity android:name=".VehicleRide" />
        <activity android:name="web.socket.handler.WebSocketHandler" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".DriverMapsActivity"
            android:label="Maps" />

        <activity android:name=".InterOrIntraCityDriverActivity" />


        <activity android:name=".InterOrIntraCityRiderActivity" />
        <activity android:name=".StartRide" />
        <activity android:name=".EndRide" />

        <activity android:name=".SplashActivity">
        <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>